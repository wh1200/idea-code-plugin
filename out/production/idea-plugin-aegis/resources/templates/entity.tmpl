#if (${PACKAGE_NAME} && ${PACKAGE_NAME} != "")package ${PACKAGE_NAME};#end

import java.io.Serializable;
import javax.persistence.Entity;

${COMMENT}
@Entity
public class ${NAME} implement Serializable {
}

